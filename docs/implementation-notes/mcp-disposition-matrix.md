# MCP tool and resource disposition matrix

Baseline inventory: all 24 tools and two resources registered at `67339a8`. Every registration is retired because none yet has a complete permission-aware Laravel/PostgreSQL data seam. The dormant runtime has no SQLite initialization, database fallback, privileged global Paperless client, or identity-less resource. The existing verified-identity guard remains available for a future seam, but no direct Paperless or local-state registration is exposed.

| Registration | Disposition / replacement seam | Identity and permission rule | Durable mutation/audit behavior | Deletion criterion |
| --- | --- | --- | --- | --- |
| `list_inbox` | **Retire**; direct Paperless reads are not the required Laravel/PostgreSQL seam | Would require verified identity and live Paperless view permission before metadata disclosure | PostgreSQL-backed, permission-filtered and redacted read only | May return only through a Laravel read seam with allowed/denied tests |
| `list_correspondents` | **Retire**; direct Paperless entity read has no Laravel/PostgreSQL data seam | Same verified identity and live permission requirement | PostgreSQL-backed, permission-filtered read only | Same |
| `list_document_types` | **Retire** for the same reason | Same | Read-only | Same |
| `list_tags` | **Retire** for the same reason | Same | Read-only | Same |
| `list_storage_paths` | **Retire** for the same reason | Same | Read-only | Same |
| `classify_document` | **Retire**; classification must start through Laravel `DocumentPipelineStarter` | Would require verified identity plus live view access before any content load | Must create durable Pipeline Run/events/audit; direct model/SQLite execution is forbidden | May return only after a Laravel permission-aware MCP Pipeline seam and allowed/denied tests exist |
| `update_document` | **Retire**; direct Paperless PATCH bypassed Laravel command/review audit semantics | Would require verified identity and live change permission before reading or mutation | Must use an approved durable Review/Command path | May return only with that Laravel seam and storage-path invariant tests |
| `list_suggestions`, `get_suggestion` | **Retire**; old source was SQLite `suggestions` | Would require verified identity and live Paperless view permission before row disclosure | PostgreSQL `review_suggestions` read only, with inaccessible rows/totals redacted | May return only via permission-filtered Laravel query seam |
| `approve_suggestion`, `reject_suggestion` | **Retire**; direct SQLite review/commit removed | Would require verified identity, MCP write grant, and live Paperless change permission | Must invoke the same Laravel Review action, durable `review_commit` Command (accept), Pipeline events and audit as UI | May return only via that Laravel seam with allowed/denied/restart tests |
| `list_tag_proposals`, `list_blacklisted_tags` | **Retire**; old source was SQLite whitelist/blacklist | Entity approval is admin-only; verified admin identity required | PostgreSQL entity-approval state, redacted structured read | May return only via admin-authorized Laravel entity seam |
| `approve_tag`, `unblacklist_tag` | **Retire** | Verified admin plus MCP write grant; Paperless permissions checked before creation/application | Same durable entity approval Command/events/audit as UI; no direct Paperless creation | Same, plus denial and restart tests |
| `list_correspondent_proposals`, `list_blacklisted_correspondents` | **Retire**; old source was SQLite | Verified admin required | PostgreSQL entity-approval read | May return only via admin-authorized Laravel entity seam |
| `approve_correspondent`, `unblacklist_correspondent` | **Retire** | Verified admin plus write grant and live Paperless checks | Durable entity approval Command/events/audit | Same, plus denial and restart tests |
| `list_doctype_proposals`, `list_blacklisted_doctypes` | **Retire**; old source was SQLite | Verified admin required | PostgreSQL entity-approval read | May return only via admin-authorized Laravel entity seam |
| `approve_doctype`, `unblacklist_doctype` | **Retire** | Verified admin plus write grant and live Paperless checks | Durable entity approval Command/events/audit | Same, plus denial and restart tests |
| `get_status` | **Retire**; mixed SQLite/global diagnostics were not permission-safe | Operational diagnostics require verified ArchiBot admin | PostgreSQL structured/redacted diagnostics only | May return only via the admin diagnostics presenter seam |
| `paperless://suggestions/pending` | **Retire**; FastMCP resource callback had no request identity and read SQLite | Cannot safely establish per-user identity with the current resource API | No mutations; PostgreSQL reads would still require permission filtering | May return only when resource requests carry verified identity and denial tests pass |
| `paperless://stats` | **Retire** for the same identityless/global-summary reason | Verified admin would be required | PostgreSQL structured/redacted aggregate only | May return only through an identity-aware admin diagnostics seam |

Historical global search/retrieval registrations (`search_documents`, `search_documents_hybrid`, `get_document`, `find_similar_documents`) were already retired by Chat/RAG containment and remain unregistered. They may only return under Issue #221's authorization-safe redesign; no SQLite vector/search replacement is introduced here.
