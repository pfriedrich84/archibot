# Laravel GUI Parity Matrix

## Source of truth

Old Python functionality is identified from:

- [`app/api_data.py`](../app/api_data.py)
- [`app/cli.py`](../app/cli.py)
- [`app/worker.py`](../app/worker.py)
- [`docs/developer/cli.md`](developer/cli.md)

Current Laravel functionality is identified from:

- [`laravel/routes/web.php`](../laravel/routes/web.php)
- [`laravel/app/Http/Controllers/`](../laravel/app/Http/Controllers/)
- [`laravel/resources/js/pages/`](../laravel/resources/js/pages/)
- [`docs/implementation-plan-laravel-job-control.md`](implementation-plan-laravel-job-control.md)

Status values: `done`, `partial`, `missing`, `deprecated`, `replaced-by-event-driven`.

## Matrix

| Area | Old functionality | Old source | Laravel route/page | Status | Missing work | Suggested test or acceptance criterion |
|---|---|---|---|---|---|---|
| Dashboard | KPIs for pending review, commits, errors, entity approvals, processed/embedded documents, inbox pending; poll/reindex progress; setup/Paperless/Ollama/embedding health; recent errors. | `get_dashboard_snapshot()`, `get_system_status()` in [`app/api_data.py`](../app/api_data.py); poll progress from [`app/worker.py`](../app/worker.py) | `GET /dashboard` -> `DashboardController` / `Dashboard.svelte`; `GET /healthz` | partial | Dashboard and `/healthz` now expose worker readiness, queue/recovery health, Paperless config and Python runtime checks. Remaining gaps are deeper phase-level historical analytics and any deployment-specific health checks beyond the current lightweight probes. | Dashboard shows actionable processing readiness without opening Worker Jobs; `/healthz` returns `ok/degraded/error` with database, queue, recovery, Paperless config, and Python runtime checks. |
| Inbox | List inbox documents, latest suggestion status, counts by status; manual/scheduled inbox polling processes OCR, embeddings, classification. | `get_inbox_snapshot()` in [`app/api_data.py`](../app/api_data.py); `poll_inbox()`, `start_poll_task()` in [`app/worker.py`](../app/worker.py); `archibot poll` in [`docs/developer/cli.md`](developer/cli.md) | `GET /inbox` -> `InboxController` / `inbox/Index.svelte`; poll controls via `POST /worker-jobs`, `POST /maintenance/poll`, `POST /admin/maintenance/worker-jobs` | partial | Inbox page is read-only and does not itself expose poll/reprocess controls; controls live in Worker Jobs/Maintenance. Consider linking to queue a poll from Inbox if product wants local action parity. | User can view Paperless inbox documents with review state; admin can start normal and forced inbox poll through Laravel without using Python CLI. |
| Review Queue | Paginated pending suggestions with confidence, correspondent/storage filters, judge verdict filter, sorting, and bulk review workflow. | `get_review_queue()` in [`app/api_data.py`](../app/api_data.py); suggestion output from [`app/cli.py`](../app/cli.py) | `GET /review`, `POST /review/bulk/accept`, `POST /review/bulk/reject` -> `ReviewSuggestionController` / `review/Index.svelte` | done | None identified for old queue parity. | Queue can filter/sort pending suggestions, bulk accept/reject latest suggestions only, and queue commit worker jobs for accepted Python-origin suggestions. |
| Review Detail | Show full suggestion details, proposed/original metadata, context documents, preview, save overrides, accept, reject, and reprocess a document. | Suggestion payload mapping and commit contract in [`app/cli.py`](../app/cli.py); review data in `get_review_queue()` | `GET /review/{reviewSuggestion}`, `GET /review/{reviewSuggestion}/preview`, `POST /review/{reviewSuggestion}/save`, `accept`, `reject`, `reprocess` -> `review/Show.svelte` | partial | Verify the page exposes raw response/original proposed snapshot if still needed for debugging; current controller focuses on user-facing fields and context. | User can inspect, edit, preview, accept, reject, and admin-reprocess a suggestion; accepted suggestions are committed through queued Laravel worker jobs. |
| Entity Approvals: Tags | Maintain tag whitelist/blacklist, approve unknown tags into Paperless, reject/unblacklist, and sync decisions back to Python state. | `get_tags_snapshot()` / `_approval_snapshot("tag")` in [`app/api_data.py`](../app/api_data.py); `cmd_sync_entity_approval()` in [`app/cli.py`](../app/cli.py) | `GET /tags`; `POST /tags/entity-approvals/{entityApproval}/approve|reject|unblacklist` -> `EntityApprovalController` / `entities/Index.svelte` | done | None identified for old tag approval parity. | Admin can approve/reject/unblacklist tags; approval creates Paperless tag and queues `sync_entity_approval`; non-admin cannot mutate. |
| Entity Approvals: Correspondents | Maintain correspondent whitelist/blacklist, approve into Paperless, reject/unblacklist, and sync decisions back to Python state. | `get_tags_snapshot()` / `_approval_snapshot("correspondent")` in [`app/api_data.py`](../app/api_data.py); `cmd_sync_entity_approval()` in [`app/cli.py`](../app/cli.py) | `GET /correspondents`; `POST /correspondents/entity-approvals/{entityApproval}/approve|reject|unblacklist` -> `EntityApprovalController` / `entities/Index.svelte` | done | None identified for old correspondent approval parity. | Admin can approve/reject/unblacklist correspondents; approval creates Paperless correspondent and queues `sync_entity_approval`. |
| Entity Approvals: Doctypes | Maintain document-type whitelist/blacklist, approve into Paperless, reject/unblacklist, and sync decisions back to Python state. | `get_tags_snapshot()` / `_approval_snapshot("doctype")` in [`app/api_data.py`](../app/api_data.py); `cmd_sync_entity_approval()` in [`app/cli.py`](../app/cli.py) | `GET /doctypes`; `POST /doctypes/entity-approvals/{entityApproval}/approve|reject|unblacklist` -> `EntityApprovalController` / `entities/Index.svelte` | done | None identified for old doctype approval parity. | Admin can approve/reject/unblacklist doctypes; approval creates Paperless document type and queues `sync_entity_approval`. |
| OCR Reviews | Generate OCR corrections during poll/reindex/process; manually create OCR review; approve/write back to Paperless, reject, restore original; optional auto write-back. | `archibot reindex-ocr`, `reindex`, `poll`, `process-doc` and OCR review payloads in [`app/cli.py`](../app/cli.py); OCR mode config in [`docs/developer/cli.md`](developer/cli.md) | `GET /ocr-reviews`, `POST /ocr-reviews`, `GET /ocr-reviews/{ocrReview}`, `approve`, `reject`, `restore` -> `OcrReviewController` / `ocr/Index.svelte`, `ocr/Show.svelte` | done | None identified for GUI parity; idempotent OCR review ingest is covered by earlier reliability phases. | OCR corrections from worker output create review records once; user can approve, reject, restore, and see write-back failures. |
| Embeddings | Inspect embedded document count and recent embedding metadata; run full reindex, OCR-only reindex, and embedding-only reindex. | `get_embeddings_snapshot()` in [`app/api_data.py`](../app/api_data.py); `cmd_reindex()`, `cmd_reindex_ocr()`, `cmd_reindex_embed()` in [`app/cli.py`](../app/cli.py) | `GET /embeddings` -> `EmbeddingsController` / `processing/Embeddings.svelte`; controls via `POST /worker-jobs`, `POST /embedding-index/build`, `POST /maintenance/reindex` | partial | Embeddings page still reads the legacy Python SQLite database; long-term event-driven target should show PostgreSQL/pgvector index state and pipeline runs consistently. | User can inspect current embedding metadata and latest reindex job; admin can start full/OCR/embedding reindex from Laravel and normal document processing is blocked while reindex is active. |
| Stats | Totals, status counts, daily commits, phase timing/error rates, confidence distribution, judge counts, chat sessions, worker counts, and legacy Python stats. | `get_stats_snapshot()` in [`app/api_data.py`](../app/api_data.py); legacy DB tables read through Python state | `GET /stats` -> `StatsController` / `stats/Index.svelte` | partial | Laravel now exposes review/entity/worker/webhook/OCR/pipeline/actor distributions, confidence and judge counts, breakdown matrices, recent activity and legacy Python stats. Remaining gap is old-style detailed phase timing/error-rate analytics. | Stats page displays operational distributions and recent activity; acceptance for full parity requires detailed phase-health/timing sections or an explicit decision to defer them to pipeline events. |
| Errors | Recent processing errors and failed/cancelled job diagnostics. | `get_recent_errors()` in [`app/api_data.py`](../app/api_data.py); `_write_error()` in [`app/worker.py`](../app/worker.py) | `GET /errors` -> `ErrorsController` / `diagnostics/Errors.svelte`; worker details via `GET /worker-jobs/{workerJob}`; webhook details via `GET /webhook-deliveries/{webhookDelivery}` | partial | Errors now include paginated/filterable worker failures, cancellations, partial failures, webhook failures/blocked/permanent errors, legacy errors, and admin retry/dismiss links where supported. Remaining gap is acknowledgement/assignment/export workflow if operators need formal incident handling. | Failed jobs, webhook failures and recent legacy errors are visible; user can navigate to operational detail pages and admins can use existing retry/dismiss controls. |
| Chat | RAG chat sessions, ask questions against Paperless/embedding context, show recent activity. | `get_chat_snapshot()` in [`app/api_data.py`](../app/api_data.py); `cmd_chat_ask()` in [`app/cli.py`](../app/cli.py); shared chat logic in `app/chat.py` | `GET /chat`, `GET /api/v1/chat`, `POST /api/v1/chat/ask`, `GET/DELETE /api/v1/chat/sessions/{session}` -> `ChatController` / `chat/Index.svelte` | partial | Current `snapshot()` returns empty `recent_activity`; old snapshot included recent commit/retry/reject audit rows. Add recent activity if the page still depends on it. | User can create, resume, ask in, and delete chat sessions; answers include sources; old recent activity is either restored or intentionally removed. |
| Settings | Show and update runtime/config schema, sensitive settings, AI provider/model settings, prompts, OCR/Telegram/MCP-related configuration. | `get_settings_schema()` in [`app/api_data.py`](../app/api_data.py); config/runtime update behavior referenced by Python config | `GET /admin/settings`, `PATCH /admin/settings`, prompt update/reset routes -> `Admin\SettingsController` / `admin/Settings.svelte`; user appearance route | partial | MCP and Telegram settings are now represented in Laravel and exported to Python runtime config. Remaining gaps are intentionally deployment-only settings such as data directory, broker/database URLs, logging and process-level ports, plus any future setting that should be classified before exposure. | Admin can edit settings and prompts through Laravel; sensitive values are masked/write-only; changes export Python runtime config and are audited. |
| Setup | Initial setup: Paperless URL/login, admin validation, tag selection, model discovery, AI provider, OCR mode, setup-token reset guard, import legacy settings. | `needs_setup()` and settings schema in [`app/api_data.py`](../app/api_data.py); setup fields from Python config | `GET /setup`, `POST /setup/paperless-tags`, `POST /setup/ollama-models`, `POST /setup` -> `SetupController` / `Setup/Index.svelte` | done | None identified for old setup parity. | Fresh install can complete setup from Laravel only; Paperless admin is verified; selected settings persist and route to Dashboard. |
| Audit Logs | Show commit/retry/reject and administrative actions; use audit history for dashboard activity and operational review. | `audit_log` reads in [`app/api_data.py`](../app/api_data.py); audit inserts throughout Python CLI/worker | `GET /admin/audit-logs` -> `Admin\AuditLogController` / `admin/AuditLogs.svelte` | partial | Admin audit page is capped at latest 100 and has no filters/export/detail view. Add filtering by event, actor, target, and date if audit review parity requires it. | Admin can see recent audited events with actor, target, metadata, IP, and timestamp; mutations from review/entity/settings/worker/maintenance are audited. |
| Worker Jobs | List/status/stop/retry persistent jobs; start poll, process document, reindex, reindex OCR, reindex embeddings; inspect logs, payload, result, progress, retry lineage, heartbeat/lease. | `archibot jobs list|status|stop|retry`, `poll`, `process-doc`, `reindex*` in [`app/cli.py`](../app/cli.py) and [`docs/developer/cli.md`](developer/cli.md) | `GET /worker-jobs`, `GET /worker-jobs/{workerJob}`, `POST /worker-jobs`, `stop`, `force-kill`, `retry` -> `WorkerJobController` / `worker/Index.svelte`, `worker/Show.svelte` | done | None identified for temporary `worker_jobs` GUI parity; long-term replacement remains `pipeline_runs`/event-driven control plane. | Admin can perform every old job CLI control from Laravel, including force options, stop/force-kill, whole-job retry, and failed-documents-only retry; details show logs and reliability metadata. |
| Maintenance | Run worker recovery, start reconciliation/reindex maintenance jobs, mark embedding index stale. Destructive reset is operator CLI-only. | Worker job commands in [`app/cli.py`](../app/cli.py); reset documented in [`docs/developer/worker-jobs.md`](developer/worker-jobs.md#cli-only-laravel-reset) | `GET /admin/maintenance`, `POST /admin/maintenance/recover-worker-jobs`, `POST /admin/maintenance/worker-jobs`; dashboard maintenance routes | partial | Reset controls were removed from the GUI by design. Operators run `archibot reset --yes`; the Python CLI delegates to Laravel/PostgreSQL and clears operational/job-control state there. | Admin can run recovery and queue maintenance jobs from Laravel; destructive reset is not exposed in the browser. |
| Webhooks | Paperless webhooks are primary event trigger; persist deliveries, dedupe, enqueue/defer processing, retry or dismiss failed deliveries; legacy `/webhook/new` and `/webhook/edit` endpoints exist. | Event-driven target in [`docs/implementation-plan-laravel-job-control.md`](implementation-plan-laravel-job-control.md); worker poll fallback in [`app/worker.py`](../app/worker.py) | `POST /api/webhooks/paperless` -> `PaperlessEventWebhookController`; `POST /webhook/new`, `/webhook/edit`; `GET /webhook-deliveries`, `GET /webhook-deliveries/{webhookDelivery}`, retry/dismiss routes | done | None identified for current webhook delivery operations parity; long-term event-driven migration should move processing from worker jobs to pipeline runs/events. | Paperless webhook delivery is durably stored/deduped and queued; failed/blocked deliveries can be listed, inspected, retried or dismissed by admin; detail pages show payloads and related pipeline events. |
| MCP | Optional MCP server with read tools, write tools gated by config, Laravel-scoped auth tokens, token creation/revocation, and MCP runtime configuration. | MCP docs and tools under `app/mcp_server.py` and `app/mcp_tools/`; config in [`docs/user/configuration.md`](user/configuration.md) and [`docs/developer/mcp.md`](developer/mcp.md) | `GET /settings/mcp-tokens`, `POST /settings/mcp-tokens`, `DELETE /settings/mcp-tokens/{mcpToken}` -> `Settings\McpTokenController` / `settings/McpTokens.svelte`; settings under `/admin/settings` | partial | No Laravel MCP server status/health page, no per-token permission UI, and no direct visibility into MCP rate limits or write-tool enablement beyond settings/token management. | User can create/revoke MCP tokens and use them for Laravel-scoped MCP auth; admin can configure MCP settings; invalid/revoked tokens are rejected by Python MCP auth. |
| Telegram | Optional Telegram notifications for suggestions/job summaries, accept/reject buttons, and Telegram-origin chat sessions. | Telegram config in `app/config.py`; notification hooks in [`app/worker.py`](../app/worker.py) and `app/pipeline/document_processing.py`; chat handling in `app/telegram_handler.py`; user docs in [`docs/user/workflow.md`](user/workflow.md) | Telegram enable/token/chat ID/poll interval settings under `GET /admin/settings`; review/chat functionality is available in Laravel web UI | partial | Admin settings and runtime export exist. No GUI exists for Telegram connection status, test notification, or Telegram session management. | If Telegram is enabled, notifications/buttons still work through Python runtime; Laravel web review remains the canonical fallback; admin can configure Telegram without editing files. |

## Follow-up checklist

- Treat every `partial` row as a candidate for a focused follow-up issue before declaring complete GUI parity.
- Prefer Laravel UI/routes as the product surface; keep Python CLI paths for debug/maintenance only.
- When replacing legacy SQLite-derived views, target the durable event-driven model: `commands`, `pipeline_runs`, `pipeline_events`, RabbitMQ/Dramatiq, PostgreSQL and pgvector.
