# Laravel GUI Parity Matrix

> **Current-state inventory, not target authority:** ADR-0017, ADR-0018, ADR-0019 and [`implementation-plan-security-architecture-hardening.md`](implementation-plan-security-architecture-hardening.md) override rows describing OCR write-back, Chat/RAG, selectable setup origins, optional webhook authentication, raw diagnostics or parallel backends. Do not extend an unsafe `done` row; follow its hardening milestone.

## Source of truth

Old Python functionality is identified from:

- [`app/api_data.py`](../app/api_data.py)
- [`app/cli.py`](../app/cli.py)
- [`app/worker.py`](../app/worker.py)
- [`docs/developer/cli.md`](developer/cli.md)

Current Laravel functionality is identified from:

- [`laravel/routes/web.php`](../laravel/routes/web.php)
- [`laravel/app/Http/Controllers/`](../laravel/app/Http/Controllers/)
- [`laravel/resources/js/pages/`](../laravel/resources/js/pages/)
- [`docs/implementation-plan-event-driven-archibot.md`](implementation-plan-event-driven-archibot.md)

Status values: `done`, `partial`, `missing`, `deprecated`, `replaced-by-event-driven`, `unsafe-current`, `hardening-pending`, `disable-pending`.

## Matrix

| Area | Old functionality | Old source | Laravel route/page | Status | Missing work | Suggested test or acceptance criterion |
|---|---|---|---|---|---|---|
| Dashboard | KPIs for pending review, commits, errors, entity approvals, processed/embedded documents, inbox pending; poll/reindex progress; setup/Paperless/Ollama/embedding health; recent errors. | `get_dashboard_snapshot()`, `get_system_status()` in [`app/api_data.py`](../app/api_data.py); poll progress from [`app/worker.py`](../app/worker.py) | `GET /dashboard` -> `DashboardController` / `Dashboard.svelte`; `GET /healthz` | partial | Dashboard and `/healthz` expose queue/recovery health, Paperless config and Python runtime checks. Remaining gaps are deeper phase-level historical analytics, exclusive Laravel transport/scheduler readiness, and any deployment-specific health checks beyond the current lightweight probes. | Dashboard shows actionable processing readiness from durable command/pipeline/actor state; `/healthz` returns `ok/degraded/error` with database, queue, recovery, Paperless config, and Python runtime checks. |
| Inbox | List inbox documents, latest suggestion status, counts by status; manual/scheduled inbox polling processes OCR, embeddings, classification. | `get_inbox_snapshot()` in [`app/api_data.py`](../app/api_data.py); `poll_inbox()`, `start_poll_task()` in [`app/worker.py`](../app/worker.py); `archibot poll` in [`docs/developer/cli.md`](developer/cli.md) | `GET /inbox` -> `InboxController` / `inbox/Index.svelte`; poll controls via `POST /maintenance/poll`, `POST /admin/maintenance/commands` | partial | Inbox page is read-only and does not itself expose poll/reprocess controls; controls live in Dashboard/Maintenance. Consider linking to queue a poll from Inbox if product wants local action parity. | User can view Paperless inbox documents with review state; admin can start normal and forced inbox poll through Laravel without using Python CLI. |
| Review Queue | Paginated pending suggestions with confidence, correspondent/storage filters, judge verdict filter, sorting, and bulk review workflow. | `get_review_queue()` in [`app/api_data.py`](../app/api_data.py); suggestion output from [`app/cli.py`](../app/cli.py) | `GET /review`, `POST /review/bulk/accept`, `POST /review/bulk/reject` -> `ReviewSuggestionController` / `review/Index.svelte` | done | None identified for old queue parity. | Queue can filter/sort pending suggestions, bulk accept/reject latest suggestions only, and queue allowlisted Laravel actor jobs for accepted Python-origin suggestions. |
| Review Detail | Show full suggestion details, proposed/original metadata, context documents, preview, save overrides, accept, reject, and reprocess a document. | Suggestion payload mapping and commit contract in [`app/cli.py`](../app/cli.py); review data in `get_review_queue()` | Existing review routes remain the current baseline. | partial | Keep debugging data structured/redacted; do not expose raw response JSON. Complete live Paperless authorization and ADR-0019 review semantics. | Authorized users can inspect/edit/accept/reject; only admins force reprocess; accepted suggestions use durable review-commit Commands. |
| Entity Approvals: Tags | Maintain tag whitelist/blacklist, approve unknown tags into Paperless, reject/unblacklist, and sync decisions back to Python state. | `get_tags_snapshot()` / `_approval_snapshot("tag")` in [`app/api_data.py`](../app/api_data.py); `cmd_sync_entity_approval()` in [`app/cli.py`](../app/cli.py) | `GET /tags`; `POST /tags/entity-approvals/{entityApproval}/approve|reject|unblacklist` -> `EntityApprovalController` / `entities/Index.svelte` | done | None identified for old tag approval parity. | Admin can approve/reject/unblacklist tags; approval creates Paperless tag and queues `sync_entity_approval`; non-admin cannot mutate. |
| Entity Approvals: Correspondents | Maintain correspondent whitelist/blacklist, approve into Paperless, reject/unblacklist, and sync decisions back to Python state. | `get_tags_snapshot()` / `_approval_snapshot("correspondent")` in [`app/api_data.py`](../app/api_data.py); `cmd_sync_entity_approval()` in [`app/cli.py`](../app/cli.py) | `GET /correspondents`; `POST /correspondents/entity-approvals/{entityApproval}/approve|reject|unblacklist` -> `EntityApprovalController` / `entities/Index.svelte` | done | None identified for old correspondent approval parity. | Admin can approve/reject/unblacklist correspondents; approval creates Paperless correspondent and queues `sync_entity_approval`. |
| Entity Approvals: Doctypes | Maintain document-type whitelist/blacklist, approve into Paperless, reject/unblacklist, and sync decisions back to Python state. | `get_tags_snapshot()` / `_approval_snapshot("doctype")` in [`app/api_data.py`](../app/api_data.py); `cmd_sync_entity_approval()` in [`app/cli.py`](../app/cli.py) | `GET /doctypes`; `POST /doctypes/entity-approvals/{entityApproval}/approve|reject|unblacklist` -> `EntityApprovalController` / `entities/Index.svelte` | done | None identified for old doctype approval parity. | Admin can approve/reject/unblacklist doctypes; approval creates Paperless document type and queues `sync_entity_approval`. |
| OCR Reviews | Local original/corrected/approved snapshots only; no Paperless content write-back, restore, retry or auto-write setting. | Laravel local OCR review module plus live `PaperlessDocumentPermissions` checks. | `/ocr-reviews` list/show/store/local approve/reject; no restore route. | contained | Milestone 0.3 authorizes every row/action against live Paperless permissions before pagination/content load, with no admin bypass; historical rows/status snapshots remain retained. | No OCR-originated Paperless content PATCH exists; denied users learn neither content nor record existence. |
| Embeddings | Inspect embedded document count and recent embedding metadata; run full reindex, OCR-only reindex, and embedding-only reindex. | `get_embeddings_snapshot()` in [`app/api_data.py`](../app/api_data.py); durable reindex actor commands | `GET /embeddings` -> `EmbeddingsController` / `processing/Embeddings.svelte`; controls via durable maintenance commands, `POST /embedding-index/build`, and `POST /maintenance/reindex` | partial | Complete full-reindex parity; the current reindex actor rebuilds the embedding index. Keep PostgreSQL/pgvector index state, commands and pipeline runs consistent. | User can inspect current embedding metadata and latest durable reindex command/run; admin can start OCR/embedding work through Laravel durable commands. Full reindex is complete only when its actor does more than the current embedding rebuild and the readiness gate remains correct. |
| Stats | Totals, status counts, daily commits, phase timing/error rates, confidence distribution, judge counts, command/pipeline/actor counts, and Python processing stats. | `get_stats_snapshot()` in [`app/api_data.py`](../app/api_data.py); durable PostgreSQL tables | `GET /stats` -> `StatsController` / `stats/Index.svelte` | partial | Remove worker distributions and legacy Python stats that depend on retired state. Remaining gap is old-style detailed phase timing/error-rate analytics from pipeline events. | Stats page displays operational distributions and recent activity; acceptance for full parity requires detailed phase-health/timing sections from pipeline events or an explicit decision to defer them. |
| Errors | Recent processing errors plus failed webhook diagnostics. | `get_recent_errors()` in [`app/api_data.py`](../app/api_data.py); failed `webhook_deliveries` | `GET /errors` -> `ErrorsController` / `diagnostics/Errors.svelte`; webhook details via `GET /webhook-deliveries/{webhookDelivery}` | partial | The page currently shows failed/blocked webhooks and `LegacyPythonState` errors only. Add durable command, pipeline-run and actor-execution diagnostics or direct users to their Operations Log views; remove the legacy source when parity exists. | Failed webhooks and legacy Python errors are visible today; full parity requires discoverable command, pipeline and actor failures with appropriate durable retry/dismiss controls. |
| Chat | Disabled for admins and non-admins; stored rows are preserved without exposure. | No Laravel page/API route or Python CLI bridge is registered; global MCP retrieval tools are absent. | Issue #221 is the only authorization-safe redesign/re-enable track. | disabled | Preserve stored rows without exposure; do not add a flag or compatibility endpoint. | Chat routes return `404`; no Python/AI call occurs; no user can read existing chat content through the disabled feature. |
| Settings | Show and update runtime/config schema, sensitive settings, AI provider/model settings, prompts, OCR/MCP-related configuration. OCR content auto-write is not configurable. | `get_settings_schema()` in [`app/api_data.py`](../app/api_data.py); config/runtime update behavior referenced by Python config | `GET /admin/settings`, `PATCH /admin/settings`, prompt update/reset routes -> `Admin\SettingsController` / `admin/Settings.svelte`; user appearance route | partial | MCP settings are represented in Laravel and exported to Python runtime config. `ocr.auto_write_back` is absent from the catalog/UI/export, and managed export removes stale `OCR_AUTO_WRITE_BACK`. Remaining gaps are intentionally deployment-only settings such as data directory, database URLs, logging and process-level ports, plus any future setting that should be classified before exposure. | Admin can edit settings and prompts through Laravel; sensitive values are masked/write-only; changes export Python runtime config and are audited; no OCR auto-write key is exposed or exported. |
| Setup | Current setup accepts a selectable Paperless URL and broad admin/staff interpretation. | Existing setup code is the hardening baseline. | Existing `/setup` routes remain current baseline. | hardening-pending | Milestone 0.6 makes deployment configuration the immutable Paperless origin across setup/login/settings/Python export, requires documented `is_superuser`, and rate-limits/validates requests. | Setup completes only against the pinned origin with a true Paperless superuser; staff-only and alternate-origin attempts fail. |
| Audit Logs | Show commit/retry/reject and administrative actions; use audit history for dashboard activity and operational review. | `audit_log` reads in [`app/api_data.py`](../app/api_data.py); audit inserts throughout Python CLI/worker | `GET /admin/audit-logs` -> `Admin\AuditLogController` / `admin/AuditLogs.svelte` | partial | Admin audit page is capped at latest 100 and has no filters/export/detail view. Add filtering by event, actor, target, and date if audit review parity requires it. | Admin can see recent audited events with actor, target, metadata, IP, and timestamp; mutations from review/entity/settings/worker/maintenance are audited. |
| Operations Log | Inspect durable commands, pipeline runs/events, actor executions, webhook deliveries and audit entries. New productive poll, process document, reindex, OCR reindex, and embedding actions are routed to durable commands/pipeline runs from Dashboard/Maintenance. | Durable Laravel/PostgreSQL operation tables | `GET /operations-log`; `/worker-jobs` removed without adding `/legacy-worker-jobs` | done | Operations Log is built from durable event-driven tables only; no compatibility data or historical row migration. | Admin can inspect operational history while new productive job controls use durable commands/pipeline/actors. |
| Maintenance | Run durable recovery, start normal/forced reconciliation, full reindex, OCR reindex, embedding build, mark embedding index stale, and manually queue one document pipeline run. Destructive reset is operator CLI-only. | Durable command/pipeline actions; reset documented in CLI docs | `GET /admin/maintenance`, durable maintenance routes, `POST /admin/maintenance/document-pipeline`; dashboard maintenance routes | partial | Reset controls were removed from the GUI by design. Operators run `archibot reset --yes`; the Python CLI delegates to Laravel/PostgreSQL and clears operational/job-control state there. Complete missing Laravel-native recovery states and remove the superseded Absurd recovery owner after parity. | Admin can run durable recovery and queue maintenance actions from Laravel; destructive reset is not exposed in the browser. |
| Webhooks | Durable ingestion/dedupe plus fail-closed secret, size and shared per-client rate controls exist on both supported aliases. | Event-driven plan and webhook/polling coordination | `POST /api/webhooks/paperless` and `POST /webhook`; delivery diagnostics remain a milestone 0.5 target. | partial | Milestone 0.4 ingress containment is implemented; milestone 0.5 still must make diagnostics admin-only and structured/redacted. | Missing/empty/wrong secret, oversized bodies and exhausted limits are rejected before persistence; valid duplicate/retry semantics remain durable. |
| MCP | Optional MCP server with read tools, write tools gated by config, Laravel-scoped auth tokens, token creation/revocation, and MCP runtime configuration. | MCP docs and tools under `app/mcp_server.py` and `app/mcp_tools/`; config in [`docs/user/configuration.md`](user/configuration.md) and [`docs/developer/mcp.md`](developer/mcp.md) | `GET /settings/mcp-tokens`, `POST /settings/mcp-tokens`, `DELETE /settings/mcp-tokens/{mcpToken}` -> `Settings\McpTokenController` / `settings/McpTokens.svelte`; settings under `/admin/settings` | partial | No Laravel MCP server status/health page, no per-token permission UI, and no direct visibility into MCP rate limits or write-tool enablement beyond settings/token management. | User can create/revoke MCP tokens and use them for Laravel-scoped MCP auth; admin can configure MCP settings; invalid/revoked tokens are rejected by Python MCP auth. |

## Follow-up checklist

- Treat every `partial` row as a candidate for a focused follow-up issue before declaring complete GUI parity.
- Prefer Laravel UI/routes as the product surface; keep Python CLI paths for debug/maintenance only.
- When replacing old views, target the durable event-driven model: `commands`, `pipeline_runs`, `pipeline_events`, `pipeline_items`, `actor_executions`, Laravel queues, PostgreSQL and pgvector.
