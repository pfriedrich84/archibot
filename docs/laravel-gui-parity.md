# Laravel GUI Parity Matrix

> **Current-state inventory, not target authority:** ADR-0017, ADR-0018, ADR-0019 and current architecture/security controls govern this matrix. Do not restore OCR write-back, Chat/RAG, selectable setup origins, optional webhook authentication, raw diagnostics or parallel backends.

## Source of truth

Historical Python functionality and its deletion disposition are recorded in [`implementation-notes/pipeline-start-caller-inventory.md`](implementation-notes/pipeline-start-caller-inventory.md). Current operator behavior is documented in [`app/cli.py`](../app/cli.py) and [`docs/developer/cli.md`](developer/cli.md).

Current Laravel functionality is identified from:

- [`laravel/routes/web.php`](../laravel/routes/web.php)
- [`laravel/app/Http/Controllers/`](../laravel/app/Http/Controllers/)
- [`laravel/resources/js/pages/`](../laravel/resources/js/pages/)
- [`docs/implementation-plan-event-driven-archibot.md`](implementation-plan-event-driven-archibot.md)

Status values: `done`, `partial`, `missing`, `deprecated`, `replaced-by-event-driven`, `contained`, `disabled`.

## Matrix

| Area | Old functionality | Old source | Laravel route/page | Status | Missing work | Suggested test or acceptance criterion |
|---|---|---|---|---|---|---|
| Dashboard | KPIs for pending review, commits, errors, entity approvals, processed/embedded documents, inbox pending; poll/reindex progress; setup/Paperless/Ollama/embedding health; recent errors. | Retired Python snapshot/worker sources; durable replacement recorded in the SQLite disposition inventory | `GET /dashboard` -> `DashboardController` / `Dashboard.svelte`; `GET /healthz` | partial | Dashboard and `/healthz` expose queue/recovery health, Paperless config and Python runtime checks. Remaining gaps are deeper phase-level historical analytics, exclusive Laravel transport/scheduler readiness, and any deployment-specific health checks beyond the current lightweight probes. | Dashboard shows actionable processing readiness from durable command/pipeline/actor state; `/healthz` returns `ok/degraded/error` with database, queue, recovery, Paperless config, and Python runtime checks. |
| Inbox | List inbox documents, latest suggestion status, counts by status; manual/scheduled inbox polling processes OCR, embeddings, classification. | Laravel/Paperless read seam and durable poll command; `archibot poll` in [`docs/developer/cli.md`](developer/cli.md) | `GET /inbox` -> `InboxController` / `inbox/Index.svelte`; poll controls via `POST /maintenance/poll`, `POST /admin/maintenance/commands` | partial | Inbox page is read-only and does not itself expose poll/reprocess controls; controls live in Dashboard/Maintenance. Consider linking to queue a poll from Inbox if product wants local action parity. | User can view Paperless inbox documents with review state; admin can start normal and forced inbox poll through Laravel without using Python CLI. |
| Review Queue | Paginated pending suggestions with confidence, correspondent/storage filters, judge verdict filter, sorting, and bulk review workflow. | PostgreSQL `review_suggestions` and Laravel Review services | `GET /review`, `POST /review/bulk/accept`, `POST /review/bulk/reject` -> `ReviewSuggestionController` / `review/Index.svelte` | done | None identified for old queue parity. | Queue can filter/sort pending suggestions, bulk accept/reject latest suggestions only, and queue allowlisted Laravel actor jobs for accepted Python-origin suggestions. |
| Review Detail | Show full suggestion details, proposed/original metadata, context documents, preview, save overrides, accept, reject, and reprocess a document. | Suggestion payload mapping and commit contract in [`app/cli.py`](../app/cli.py); review data in PostgreSQL `review_suggestions` | Existing review routes remain the current baseline. | done | None. Debugging data remains structured/redacted; raw response JSON is not exposed. | Authorized users can inspect/edit/accept/reject with live Paperless authorization; only admins force reprocess; accepted suggestions use durable review-commit Commands. |
| Entity Approvals: Tags | Maintain PostgreSQL-owned tag approval/blacklist decisions and apply approved tags retroactively. | Durable `entity_approvals`, `review_suggestions`, Commands, events and audits; no Python/SQLite sync actor | `GET /tags`; `POST /tags/entity-approvals/{entityApproval}/approve|reject|unblacklist` -> `EntityApprovalController` / `entities/Index.svelte` | done | None. | Admin decisions create a durable Command before mutation, update PostgreSQL suggestions, patch only matching committed Paperless documents, and never initialize or read `classifier.db`. |
| Entity Approvals: Correspondents | Maintain PostgreSQL-owned correspondent approval/blacklist decisions and retroactive application. | Durable `entity_approvals`, `review_suggestions`, Commands, events and audits; no Python/SQLite sync actor | `GET /correspondents`; approval/reject/unblacklist routes -> `EntityApprovalController` | done | None. | Admin decisions use the Laravel/PostgreSQL entity seam and patch matching committed Paperless documents. |
| Entity Approvals: Doctypes | Maintain PostgreSQL-owned document-type approval/blacklist decisions and retroactive application. | Durable `entity_approvals`, `review_suggestions`, Commands, events and audits; no Python/SQLite sync actor | `GET /doctypes`; approval/reject/unblacklist routes -> `EntityApprovalController` | done | None. | Admin decisions use the Laravel/PostgreSQL entity seam and patch matching committed Paperless documents. |
| OCR Reviews | Local original/corrected/approved snapshots only; no Paperless content write-back, restore, retry or auto-write setting. | Laravel local OCR review module plus live `PaperlessDocumentPermissions` checks. | `/ocr-reviews` list/show/store/local approve/reject; no restore route. | contained | Every row/action is authorized against live Paperless permissions before pagination/content load, with no admin bypass; historical rows/status snapshots remain retained. | No OCR-originated Paperless content PATCH exists; denied users learn neither content nor record existence. |
| Embeddings | Inspect embedded document count and recent embedding metadata; run full reindex, OCR-only reindex, and embedding-only reindex. | PostgreSQL/pgvector embedding state and durable reindex actor commands | `GET /embeddings` -> `EmbeddingsController` / `processing/Embeddings.svelte`; controls via durable maintenance commands, `POST /embedding-index/build`, and `POST /maintenance/reindex` | partial | Complete full-reindex parity; the current reindex actor rebuilds the embedding index. Keep PostgreSQL/pgvector index state, commands and pipeline runs consistent. | User can inspect current embedding metadata and latest durable reindex command/run; admin can start OCR/embedding work through Laravel durable commands. Full reindex is complete only when its actor does more than the current embedding rebuild and the readiness gate remains correct. |
| Stats | Durable review, entity, webhook, pipeline, actor and daily activity distributions. | Durable PostgreSQL tables | `GET /stats` -> `StatsController` / `stats/Index.svelte` | partial | Legacy local statistics are removed. Remaining gap is detailed phase timing/error-rate analytics derived from pipeline events. | Stats page displays operational distributions and recent activity; acceptance for full parity requires detailed phase-health/timing sections from pipeline events or an explicit decision to defer them. |
| Errors | Failed durable webhook diagnostics. | PostgreSQL `webhook_deliveries` | `GET /errors` -> `ErrorsController` / `diagnostics/Errors.svelte`; webhook details via `GET /webhook-deliveries/{webhookDelivery}` | partial | Add durable command, pipeline-run and actor-execution diagnostics or direct users to their Operations Log views. | Failed webhooks are visible; full parity requires discoverable command, pipeline and actor failures with appropriate durable retry/dismiss controls. |
| Chat | Disabled for admins and non-admins; stored rows are preserved without exposure. | No Laravel page/API route or Python CLI bridge is registered; global MCP retrieval tools are absent. | Issue #221 is the only authorization-safe redesign/re-enable track. | disabled | Preserve stored rows without exposure; do not add a flag or compatibility endpoint. | Chat routes return `404`; no Python/AI call occurs; no user can read existing chat content through the disabled feature. |
| Settings | Show and update runtime/config schema, sensitive settings, AI provider/model settings, prompts, OCR/MCP-related configuration. OCR content auto-write is not configurable. | Laravel setting catalog/export and Python runtime config | `GET /admin/settings`, `PATCH /admin/settings`, prompt update/reset routes -> `Admin\SettingsController` / `admin/Settings.svelte`; user appearance route | partial | MCP settings are represented in Laravel and exported to Python runtime config. `ocr.auto_write_back` is absent from the catalog/UI/export, and managed export removes stale `OCR_AUTO_WRITE_BACK`. Remaining gaps are intentionally deployment-only settings such as data directory, database URLs, logging and process-level ports, plus any future setting that should be classified before exposure. | Admin can edit settings and prompts through Laravel; sensitive values are masked/write-only; changes export Python runtime config and are audited; no OCR auto-write key is exposed or exported. |
| Setup | Deployment-pinned Paperless origin, true Paperless superuser verification, bounded network requests and deferred provider editing. | Laravel setup services and `CanonicalPaperlessOrigin`. | `/setup` routes with a read-only Paperless destination. | done | None. | Setup completes only against the pinned origin with a true Paperless superuser; staff-only and alternate-origin attempts fail. |
| Audit Logs | Show commit/retry/reject and administrative actions; use audit history for dashboard activity and operational review. | PostgreSQL `audit_logs` written by Laravel services and durable Python lifecycle seams | `GET /admin/audit-logs` -> `Admin\AuditLogController` / `admin/AuditLogs.svelte` | partial | Admin audit page is capped at latest 100 and has no filters/export/detail view. Add filtering by event, actor, target, and date if audit review parity requires it. | Admin can see recent audited events with actor, target, metadata, IP, and timestamp; mutations from review/entity/settings/worker/maintenance are audited. |
| Operations Log | Inspect durable commands, pipeline runs/events, actor executions, webhook deliveries and audit entries. New productive poll, process document, reindex, OCR reindex, and embedding actions are routed to durable commands/pipeline runs from Dashboard/Maintenance. | Durable Laravel/PostgreSQL operation tables | `GET /operations-log`; `/worker-jobs` removed without adding `/legacy-worker-jobs` | done | Operations Log is built from durable event-driven tables only; no compatibility data or historical row migration. | Admin can inspect operational history while new productive job controls use durable commands/pipeline/actors. |
| Maintenance | Run durable recovery, normal/forced reconciliation, full/OCR/embedding reindex, one-document processing, and confirmed reset. | Shared Laravel Command/Pipeline/reset services used by both CLI and UI | `GET /admin/maintenance`, durable maintenance/document/reset routes | done | None for command equivalence. | CLI and admin UI produce equivalent durable commands, events, audits, progress and outcomes across restart; reset requires explicit confirmation and uses the same backend. |
| Webhooks | Durable ingestion/dedupe plus fail-closed secret, size and shared per-client rate controls exist on both supported aliases. | Event-driven plan and webhook/polling coordination | `POST /api/webhooks/paperless` and `POST /webhook`; admin-only structured/redacted delivery diagnostics. | done | None. | Missing/empty/wrong secret, oversized bodies and exhausted limits are rejected before persistence; valid duplicate/retry semantics remain durable and diagnostics reveal no raw payloads or secrets. |
| MCP | Optional MCP process and Laravel-scoped token/configuration surfaces; no tools, resources or templates are currently registered. | MCP docs and retired-registration matrix under `app/mcp_server.py`, `app/mcp_tools/`, and [`docs/developer/mcp.md`](developer/mcp.md) | `GET /settings/mcp-tokens`, `POST /settings/mcp-tokens`, `DELETE /settings/mcp-tokens/{mcpToken}` -> `Settings\McpTokenController` / `settings/McpTokens.svelte`; settings under `/admin/settings` | contained | Registrations may return only after permission-aware Laravel/PostgreSQL seams and allowed/denied identity tests exist. | Runtime inventory remains empty; invalid/revoked tokens fail closed; no identity-less document or system data is exposed. |

## Follow-up checklist

- Treat every `partial` row as a candidate for a focused follow-up issue before declaring complete GUI parity.
- Prefer Laravel UI/routes as the product surface; keep Python CLI paths for debug/maintenance only.
- When replacing old views, target the durable event-driven model: `commands`, `pipeline_runs`, `pipeline_events`, `pipeline_items`, `actor_executions`, Laravel queues, PostgreSQL and pgvector.
