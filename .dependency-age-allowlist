# Exceptions to the 3-day minimum dependency age rule.
# Only security patches from established packages should be listed here.
# Format: package==version  # CVE or justification
#
# Each entry MUST include the CVE number being fixed and should be
# removed once the version is older than 3 days.
#
# (no active exceptions — cryptography==46.0.7 is now >3 days old)
annotated-types==0.8.0  # temporary-supply-chain-quarantine-exception-released-2026-07-23-remove-after-2026-07-26
